node_modules/
dist/
dist-ssr/
*.local

.DS_Store
.vscode/*
!.vscode/extensions.json
.idea/

npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.tsbuildinfo

.env
.env.*
!.env.example
# Tracked deliberately: it holds only the Supabase URL and publishable key,
# both of which ship inside the client bundle anyway. The deployed build reads
# them at build time, so ignoring this file would deploy an app with no
# connection at all.
!.env.production
